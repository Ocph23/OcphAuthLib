@if(Errors!=null){
    <ul>
        @foreach (var item in Errors)
        {
            <li style="color:orangered;font-size:small">@item.Value</li>
        }
    </ul>
}

@code {
    [Parameter]
    public Dictionary<string,string> Errors {get;set;}
}
